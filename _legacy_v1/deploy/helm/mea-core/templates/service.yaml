apiVersion: v1
kind: Service
metadata:
  name: {{ include "mea-core.fullname" . }}
  labels:
    {{- include "mea-core.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mea-core.selectorLabels" . | nindent 4 }}
